# Example environment variables for backend
# Replace values as appropriate

# MongoDB connection string (prefer IPv4 for local dev)
# Local (default): mongodb://127.0.0.1:27017/friendly_notebook
# Atlas example: mongodb+srv://<user>:<password>@cluster0.example.mongodb.net/friendly_notebook?retryWrites=true&w=majority
MONGO_URI=mongodb://127.0.0.1:27017/friendly_notebook

# JWT secret used to sign session tokens
JWT_SECRET=please_change_this_secret

# Optional: number of connection attempts
MONGO_CONNECT_ATTEMPTS=5
MONGO_RETRY_DELAY_MS=2000
