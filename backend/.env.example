# MongoDB Connection (Required)
# Use 'mongodb://127.0.0.1:27017/your_db' for local
# Use 'mongodb+srv://user:pass@...' for Cloud/Atlas
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname

# Server Port (Optional, Cloud platforms set this automatically)
PORT=5000

# Authentication (Required for consistency)
JWT_SECRET=your_secure_secret_key_here

# Frontend URL (For CORS policies if deployed separately)
# If using the Monolithic deploy (serving static build), this is less critical but good praxis
REACT_APP_API_URL=https://your-backend-app.onrender.com
