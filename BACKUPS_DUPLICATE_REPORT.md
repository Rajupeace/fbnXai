Summary of backup duplicate scan

- Scope: `backups/` (all backup folders) vs `backups/latest`
- Goal: identify identical `.json` files and recommend canonical copies (prefer `backups/latest`).

1) Empty-array duplicate group (common filenames found with identical content `[]`):
- database: `AdminData.json`, `AdminMessage.json`, `attendances.json`, `courses.json`, `FacultyData.json`, `faculties.json`, `materials.json`, `messages.json`, `schedule.json`, `StudentData.json`, `students.json`, `todos.json`
- file_storage: `admin.json`, `chatHistory.json`, `courses.json`, `faculty.json`, `materials.json`, `messages.json`, `schedule.json`, `students.json`, `todos.json`

Recommendation: keep the copies under `backups/latest/` as canonical; older backups with identical empty content can be removed to reduce clutter.

2) Substantive files (do NOT remove unless you confirm):
- `backups/latest/file_storage/materials.json` — large populated materials export (canonical)
- `backups/latest/file_storage/messages.json` — populated messages (canonical)
- `backups/latest/database/*` — recent non-empty database exports (prefer these as canonical)

3) Safe dry-run PowerShell to list duplicate empty JSONs outside `backups/latest` (no deletions):

```powershell
# find JSON files under backups that are NOT in backups/latest and whose content is exactly []
Get-ChildItem -Path .\backups -Recurse -Filter *.json |
  Where-Object { $_.FullName -notmatch "\\backups\\latest\\" -and (Get-Content $_.FullName -Raw).Trim() -eq '[]' } |
  ForEach-Object { Write-Output "Would remove: $($_.FullName)" }
```

4) Safe dry-run using Remove-Item with `-WhatIf` (shows proposed deletions):

```powershell
Get-ChildItem -Path .\backups -Recurse -Filter *.json |
  Where-Object { $_.FullName -notmatch "\\backups\\latest\\" -and (Get-Content $_.FullName -Raw).Trim() -eq '[]' } |
  Remove-Item -WhatIf
```

5) Actual deletion (only run after you confirm and after taking a copy or committing):

```powershell
Get-ChildItem -Path .\backups -Recurse -Filter *.json |
  Where-Object { $_.FullName -notmatch "\\backups\\latest\\" -and (Get-Content $_.FullName -Raw).Trim() -eq '[]' } |
  Remove-Item -Verbose
```

Notes & safety:
- I do NOT delete anything automatically without your approval.
- These commands target only `.json` files whose content equals `[]` and skip anything under `backups/latest`.
- If you want me to be more conservative, I can prepare an explicit file list first and show it for confirmation.

Next actions (pick one):
- Confirm and run the dry-run listing (`#3`) so you can review the list.
- Confirm and run the `-WhatIf` deletion (`#4`) to preview Delete operations.
- Confirm and run actual deletion (`#5`) to remove the older empty JSON backups.
- Ask me to build an explicit CSV list of files that would be removed for review.

Report generated by the scan tool.
